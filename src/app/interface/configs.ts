export interface ConfigsInterface {
  serverURL: string;
  auth: string;
  loginURL: string;
  registryURL: string;
  refreshURL: string;
  dictionary: string;
  testing: string;
  isAuthorized:string
}
